<% if(errors.length > 0) { %>
    <div class="errors">
        <h1>
            <% errors.forEach(erro =>{ %>
                    <%= erro %></br>
                <% }); %>
        </h1>
    </div>
<% } %>
